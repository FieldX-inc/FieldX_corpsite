---
name: seo-column-pipeline
description: 1本の日本語コラム記事を、企画、キーワード選定、SERP分析、brief作成、構成設計、本文執筆、CTA設計、公開前監査まで一気通貫で進める orchestrator skill。SEO記事制作全体をまとめて進めたいとき、どの specialist skill をどの順で使うかを判断したいときに使う。
---

# SEO記事パイプライン

## 概要

この skill は司令塔として振る舞う。  
自分で全部を書くのではなく、必要に応じて既存 skill を順番に使い、1本の記事を publish-ready に近い状態まで持っていく。

使う specialist skill:

- `$keyword-strategy`
- `$serp-analysis`
- `$content-outline`
- `$seo-writer`
- `$conversion-cta`
- `$quality-audit`

## 受け取る入力

最低限ほしい入力:

- テーマまたは主題
- 想定読者
- 事業上の目的
- 想定 CV

あると精度が上がる入力:

- 既存記事との関係
- 避けたい表現
- 参照すべき社内情報
- publish 期限

入力が不足していても止まらず、`AGENTS.md`、`briefs/`、`references/`、`content/` から補完する。

## 基本フロー

1. テーマ確認
2. キーワード戦略
3. SERP 分析
4. brief 作成
5. 構成設計
6. 本文執筆
7. CTA 設計
8. 品質監査
9. 最終出力の整理

## 各ステップ

### 1. テーマ確認

- 記事の主題を 1 文で定義する。
- 読者が記事を読む理由を 1 文で定義する。
- 記事読後に取ってほしい行動を 1 つ決める。

ここが曖昧なら、その後の工程も曖昧になる。先に固定する。

### 2. キーワード戦略

`$keyword-strategy` を使って、主軸キーワードと補助キーワードを決める。  
記事化を見送るべきなら、ここで止めてもよい。

### 3. SERP 分析

`$serp-analysis` を使って、現在の検索意図と差別化の勝ち筋を整理する。  
最新情報が必要な論点はこの段階でフラグを立てる。

### 4. brief 作成

`templates/article-brief.md` に沿って brief をまとめる。  
保存先は `briefs/` とし、ファイル名は `YYYY-MM-DD-topic.md` を基本にする。

brief に必ず含める:

- 主軸キーワード
- 読者像
- 検索意図
- 記事の勝ち筋
- 必須論点
- 避ける論点
- CTA 方針
- 検証が必要な主張

### 5. 構成設計

`$content-outline` を使って、brief から構成案を作る。  
出力は `templates/outline-template.md` に沿わせる。

### 6. 本文執筆

`$seo-writer` を使って、構成案から本文を作る。  
このとき以下も一緒に作る。

- タイトル案
- meta description 案
- FAQ 案
- 図解が必要な箇所

### 7. CTA 設計

`$conversion-cta` を使って、記事の温度感に合う導線を設計する。  
CTA は本文を書き終えてから後付けするのではなく、記事の結論と矛盾しないように整える。

### 8. 品質監査

`$quality-audit` を使って公開判定を行う。  
重大な問題があれば修正し、必要なら再監査する。

### 9. 最終出力の整理

最低限まとめるもの:

- brief
- 構成案
- 本文ドラフト
- title / description 候補
- CTA
- QA 結果

## sub-agent を使ってよい場面

sub-agent は並列で効果が出る調査だけに使う。

使ってよい例:

- 上位競合ページの要約収集
- 関連質問の抽出
- 一次情報ソース候補の収集
- repo 内の内部リンク候補探索

使わない例:

- 最終的な記事方針の決定
- ブランド表現の判断
- 公開可否の最終判断

最終統合は必ず自分で行う。

## 出力形式

最後は次の順で返す。

1. 記事の要約
2. 採用キーワード
3. brief の要点
4. 構成
5. 本文ドラフト
6. CTA
7. QA 結果
8. 未解決の確認事項

## 運用ルール

- 公開ソースが `microCMS` であることを前提にする。
- 生成物はそのまま publish 済みとみなさない。
- 競合比較、最新統計、法規制、価格、制度、プロダクト仕様は検証前提とする。
- 日本語のみで出力する。
- 既存記事との cannibalization が強い場合は、新規記事ではなく改稿を提案してよい。
